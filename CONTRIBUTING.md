# 贡献指南

感谢你帮助改进“基于双 RDK X5 异构协同的材料合成 AI 预测与多机具身实验助理机器人”。这个仓库同时包含材料智能、边缘推理、ROS 2、双机械臂与嵌入式固件；任何贡献都必须先保证事实可核查和物理系统安全。

## 适合贡献的内容

- 修正文档、无效链接、配置示例和可复现性问题。
- 增加不接触真实硬件的单元测试、fixture 或离线回放。
- 改进模型、数据与第三方依赖的来源和许可证记录。
- 在明确的模块边界内修复缺陷或提升可维护性。

请先提交 issue 讨论会改变协议、模型合同、硬件接线、执行流程或数据格式的提案。安全漏洞请按 [SECURITY.md](SECURITY.md) 私下报告。

## 硬约束

- 不提交 API key、密码、令牌、Cookie、私钥、设备身份、私网地址、个人绝对路径或个人数据。
- 不把 `shadow`、`replay`、`sim-only`、固定张量测试或编译器估算写成真实闭环效果。
- 不把公开数据集写成团队产线或实验室采集数据。
- 不通过 Pull Request 改写已归档的验收回执；新结果应追加新的、内容可寻址的证据。
- 不让测试、示例或安装步骤默认连接相机、串口、GPIO、机器人 SDK 或运动话题。
- 任何可能动作的路径必须默认拒绝，并遵循 [物理安全规范](docs/safety/PHYSICAL_SAFETY.md)。

## 工作流程

1. Fork 仓库并从最新主分支创建主题分支。
2. 仅在示例配置中填写本地变量；不要提交真实 `.env`。
3. 先运行目标模块自身 README 中的离线检查，再运行根 README 列出的发布审计、链接、奖项、SBOM、媒体与零硬件测试门。
4. 更新受影响的文档、测试、数据来源和 [证据矩阵](docs/evidence/CLAIM_MATRIX.csv)。
5. 提交内容聚焦的 Pull Request，说明测试环境、实际运行的命令、结果和未验证项。

## 提交与评审

建议使用简明的 Conventional Commits 前缀，如 `docs:`、`fix:`、`test:`、`feat:`。PR 描述至少包含：

- 改了什么以及为什么；
- 是否影响硬件、网络、数据或模型合同；
- 已运行的检查及原始结果；
- 新增公开断言对应的证据路径；
- 第三方内容的来源、版本与许可证。

维护者可以拒绝缺少复现证据、掩盖失败状态或扩大物理权限的修改。
